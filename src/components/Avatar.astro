---
import { Image } from 'astro:assets';

interface Props {
  avatar: string;
  username: string;
}

const { username, avatar } = Astro.props;
---

<Image
  src={avatar}
  alt={`Picture of ${username}`}
  width="4096"
  height="4096"
  class="h-16 w-16 rounded-full"
  quality="max"
/>
